

import 'package:flutter/material.dart';

class TwoFragment extends StatelessWidget {
  const TwoFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Two Fragment'),
      ),
      body: const Center(
        child: Text('Two Fragment'),
      ),
    );
  }
}


















